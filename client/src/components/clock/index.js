import PropTypes from 'prop-types'
import React from 'react'
import './Clock.css'

const propTypes = {
    //
}

const Clock = () => (
    <div className="Clock">
        Clock
    </div>
)

Clock
.propTypes = propTypes

export default Clock
