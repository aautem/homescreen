import React from 'react'

import './Calendar.css'

const Calendar = ({ children, color, flexBasis }) => (
  <section
    className="Calendar"
    style={{ background: 'blue', flexBasis: `100%` }}
  >
    Hi
  </section>
)

export default Calendar
